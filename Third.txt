 git checkout -b develop git checkout -b develop git checkout -b develop git checkout -b develop
  git checkout -b develop
   git checkout -b develop
    git checkout -b develop
	 git checkout -b develop
	  git checkout -b develop
	   git checkout -b develop